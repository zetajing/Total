<UserControl x:Class="IndustrialCommDemo.Views.StorageTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock FontSize="22" FontWeight="SemiBold" Foreground="#17324D" Text="本地数据目录" />
        <TextBlock Grid.Row="1" Margin="0,10,0,14" Foreground="#64748B" TextWrapping="Wrap" Text="日志、界面状态和缓存统一保存在该目录。禁止选择 C 盘；默认使用软件目录，若软件在 C 盘则自动选择第一个可用的非 C 固定磁盘。" />
        <Grid Grid.Row="2"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="120"/></Grid.ColumnDefinitions>
            <TextBox x:Name="DataDirectoryTextBox" Padding="8" FontFamily="Consolas" />
            <Button Grid.Column="1" Margin="10,0,0,0" Padding="10" Background="#2563EB" Foreground="White" BorderThickness="0" Click="ApplyDataDirectoryButton_Click" Content="应用目录" />
        </Grid>
        <TextBlock x:Name="DataDirectoryHintTextBlock" Grid.Row="3" Margin="0,14,0,0" Foreground="#0F766E" TextWrapping="Wrap" />
    </Grid>
</UserControl>
